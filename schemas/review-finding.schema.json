{
  "$defs": {
    "ConcernDisposition": {
      "enum": [
        "pending",
        "addressed",
        "disagreed_with_reason",
        "accepted_as_limitation"
      ],
      "title": "ConcernDisposition",
      "type": "string"
    },
    "ReviewSeverity": {
      "enum": [
        "P0",
        "P1",
        "P2"
      ],
      "title": "ReviewSeverity",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "category": {
      "title": "Category",
      "type": "string"
    },
    "disposition": {
      "$ref": "#/$defs/ConcernDisposition",
      "default": "pending"
    },
    "disposition_rationale": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Disposition Rationale"
    },
    "finding_id": {
      "title": "Finding Id",
      "type": "string"
    },
    "message": {
      "title": "Message",
      "type": "string"
    },
    "schema_version": {
      "default": 1,
      "title": "Schema Version",
      "type": "integer"
    },
    "severity": {
      "$ref": "#/$defs/ReviewSeverity"
    }
  },
  "required": [
    "finding_id",
    "severity",
    "category",
    "message"
  ],
  "title": "ReviewFinding",
  "type": "object"
}
